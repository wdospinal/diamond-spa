# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# persisted bookings (local / self-hosted)
/data/bookings.json
/data/ledger.json
/data/.seed-expenses-v1
# funnel counters fallback (local / self-hosted; prod uses Vercel KV)
/data/funnel-daily.json
# web-push subscriptions fallback (local / self-hosted; prod uses Vercel KV) — contains user device keys, never commit
/data/push-subs.json
